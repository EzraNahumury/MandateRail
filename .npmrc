# Generated @daml.js/* packages reference each other by plain version, not the
# workspace: protocol. Prefer/link the local workspace copies so they resolve
# to daml.js/* instead of being fetched from the npm registry (where they do
# not exist).
link-workspace-packages=true
prefer-workspace-packages=true
